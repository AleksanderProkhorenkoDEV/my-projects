import { Grid } from "@/components/grid-layout/grid-layout";
import cards from "@/components/grid-layout/Projects/Cards";

const Formacion = async () => {

    return (
        <Grid cards={cards} />
    )
}

export default Formacion